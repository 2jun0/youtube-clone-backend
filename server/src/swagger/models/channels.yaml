channels:
  type: object
  properties:
    id:
      type: integer
    createdAt:
      type: string
      format: date
    updatedAt:
      type: string
      format: date
    name:
      type: string
    description:
      type: string
    accountId:
      type: integer
